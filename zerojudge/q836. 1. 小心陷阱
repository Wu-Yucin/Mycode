#include<bits/stdc++.h>
using namespace std;

//k:初始生命值,x1 or y1:兩種倍數,x2 or y2:減少的生命值,p:當前位置
int k,x1,y1,x2,y2,p=0;
int main(){
    cin >> k;
    cin >> x1 >> y1;
    cin >> x2 >> y2;
    p += k;
    if (p%x1==0){
        k -= y1;
    }
    if (p%x2==0){
        k -= y2;
    }

    return 0;
}

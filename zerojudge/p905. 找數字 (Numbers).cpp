#include<bits/stdc++.h>
using namespace std;

//n:正整數,is_prime:是否為質數
int n;
bool is_prime=true;
int main(){
    cin >> n;
    for(int i=n ; i>0 ; ++i){
        if (%i == 0){
            cout << i;
            break;
        }
    }
    for (int j=n ; j>0 ; ++j){
        if (sqrt(j) = int){
            cout << j;
            break;
        }
    }
    for (int w=n ; w>0 ; ++w){
        if (sqrt(sqrt(w)) = int){
            cout << w;
            break;
        }
    }
    return 0;
}
